# explain.md
## Explanation of 01-variables.js 

📁 **ไฟล์:** [01-variables.js](01-variables.js)  เป็นการฝึกพื้นฐานภาษา JavaScript เกี่ยวกับตัวแปร ชนิดข้อมูล และ object  
โดยใช้คำสั่ง `console.log()` เพื่อแสดงผลลัพธ์ใน console และอธิบายการทำงานของโค้ดแต่ละส่วน

---

## 6. Challenge: Person Object

### ผลลัพธ์
Full name: Alice Smith
Info: Alice Smith, Age: 20, GPA: 3.8
Courses: HTML, CSS, JavaScript


### คำอธิบาย
- Object `student` เก็บข้อมูลและ method
- `this` ใช้อ้างถึง object ปัจจุบัน
- method สามารถเรียกใช้ข้อมูลภายใน object ได้

---

## สรุป
ไฟล์ `01-variables.js` แสดงให้เห็นการทำงานพื้นฐานของ JavaScript เหมาะสำหรับผู้เริ่มต้นและใช้เป็นเอกสารอ้างอิง

# explain.md
## Explanation of 02-functions.js

📁 **ไฟล์:** [02-functions.js](02-functions.js)  
ไฟล์นี้แสดงการใช้ฟังก์ชันใน JavaScript ทั้งการ **คืนค่าเป็น Object** และการใช้ **ฟังก์ชันเป็นพารามิเตอร์ (Callback)**

---

## 8. Returning Objects

### ผลลัพธ์
Returning Objects:  
{
  firstName: 'John',  
  lastName: 'Doe',  
  age: 30,  
  email: 'john.doe@example.com',  
  getFullName: [Function: getFullName],  
  getAge: [Function: getAge]  
}  
Email: john.doe@example.com  
Full name: John Doe  

### คำอธิบาย
- ฟังก์ชัน `createUser` คืนค่าเป็น Object ที่สร้างขึ้นใหม่  
- การเขียน `{ firstName, lastName, age }` เป็น **shorthand property**  
- `email` สร้างจากชื่อและนามสกุลแบบตัวพิมพ์เล็ก  
- `getFullName()` และ `getAge()` เป็น **method** ใน Object ใช้ `this` อ้างถึง property ภายใน  
- เรียก `newUser.getFullName()` จะได้ `"John Doe"`

---

## 9. Function as Parameter (Callback)

### ผลลัพธ์
Callback Function:  
Original: [1, 2, 3, 4, 5]  
Doubled: [2, 4, 6, 8, 10]  
Squared: [1, 4, 9, 16, 25]  

### คำอธิบาย
- ฟังก์ชัน `processArray` รับ array และ **callback function**  
- วนลูปทุก element ของ array และเรียก `callback(item)`  
- ตัวอย่าง `doubled` ใช้ `(x) => x * 2`  
- ตัวอย่าง `squared` ใช้ `(x) => x * x`  
- คืนค่าเป็น array ใหม่ที่ผ่าน callback

---

## สรุป
ไฟล์ `02-functions.js` แสดงให้เห็น:
- การคืนค่า Object จากฟังก์ชัน พร้อม method และ shorthand property  
- การส่งฟังก์ชันเป็นพารามิเตอร์ (callback) เพื่อประมวลผลข้อมูลอย่างยืดหยุ่น  
เหมาะสำหรับผู้เริ่มต้นเรียนรู้ฟังก์ชันขั้นสูงใน JavaScript
