## Explanation of 01-variables.js

📁 **ไฟล์:** [01-variables.js](01-variables.js)  
ไฟล์นี้ฝึกพื้นฐาน JavaScript เกี่ยวกับตัวแปร ชนิดข้อมูล และ object  
โดยใช้ `console.log()` แสดงผลลัพธ์และอธิบายการทำงานของโค้ดแต่ละส่วน

---

## 6. Challenge: Person Object

### ผลลัพธ์

Student object:  
{
firstName: 'Alice',  
 lastName: 'Smith',  
 age: 20,  
 gpa: 3.8,  
 courses: [ 'HTML', 'CSS', 'JavaScript' ],  
 isActive: true,  
 getFullName: [Function: getFullName],  
 getInfo: [Function: getInfo]  
}  
Full name: Alice Smith  
Info: Alice Smith, Age: 20, GPA: 3.8  
Courses: HTML, CSS, JavaScript

### คำอธิบาย

- Object `student` เก็บข้อมูลนักเรียนและ **method**
- Method `getFullName()` ใช้ `this` อ้างถึง property ภายใน object
- Method `getInfo()` เรียก `getFullName()` และรวมข้อมูลอื่น ๆ
- `courses.join(", ")` แปลง array เป็น string เพื่อแสดงผล

---

## สรุป

ไฟล์ `01-variables.js` แสดงตัวอย่างการสร้าง **object** และ **method**  
ช่วยให้เข้าใจการจัดการข้อมูลในรูปแบบ object ใน JavaScript

## Explanation of 02-functions.js

📁 **ไฟล์:** [02-functions.js](02-functions.js)  
ไฟล์นี้แสดงการใช้ฟังก์ชันใน JavaScript ทั้งการ **คืนค่าเป็น Object** และการใช้ **ฟังก์ชันเป็นพารามิเตอร์ (Callback)**

---

## 8. Returning Objects

### ผลลัพธ์

Returning Objects:  
{
firstName: 'John',  
 lastName: 'Doe',  
 age: 30,  
 email: 'john.doe@example.com',  
 getFullName: [Function: getFullName],  
 getAge: [Function: getAge]  
}  
Email: john.doe@example.com  
Full name: John Doe

### คำอธิบาย

- ฟังก์ชัน `createUser` คืนค่าเป็น Object ที่สร้างขึ้นใหม่
- การเขียน `{ firstName, lastName, age }` เป็น **shorthand property**
- `email` สร้างจากชื่อและนามสกุลแบบตัวพิมพ์เล็ก
- `getFullName()` และ `getAge()` เป็น **method** ใน Object ใช้ `this` อ้างถึง property ภายใน
- เรียก `newUser.getFullName()` จะได้ `"John Doe"`

---

## 9. Function as Parameter (Callback)

### ผลลัพธ์

Callback Function:  
Original: [1, 2, 3, 4, 5]  
Doubled: [2, 4, 6, 8, 10]  
Squared: [1, 4, 9, 16, 25]

### คำอธิบาย

- ฟังก์ชัน `processArray` รับ array และ **callback function**
- วนลูปทุก element ของ array และเรียก `callback(item)`
- ตัวอย่าง `doubled` ใช้ `(x) => x * 2`
- ตัวอย่าง `squared` ใช้ `(x) => x * x`
- คืนค่าเป็น array ใหม่ที่ผ่าน callback

---

## สรุป

ไฟล์ `02-functions.js` แสดงให้เห็น:

- การคืนค่า Object จากฟังก์ชัน พร้อม method และ shorthand property
- การส่งฟังก์ชันเป็นพารามิเตอร์ (callback) เพื่อประมวลผลข้อมูลอย่างยืดหยุ่น  
  เหมาะสำหรับผู้เริ่มต้นเรียนรู้ฟังก์ชันขั้นสูงใน JavaScript

## Explanation of 03-control-flow.js

📁 **ไฟล์:** [03-control-flow.js](03-control-flow.js)  
ไฟล์นี้แสดงตัวอย่างการใช้ **Short-Circuit Evaluation** และ **Form Validation**  
เพื่อควบคุมการไหลของโปรแกรมและตรวจสอบค่าที่เข้ามา

---

## 5. Short-Circuit Evaluation

### ผลลัพธ์

Short-Circuit Evaluation:  
User name: John  
User profile: undefined

### คำอธิบาย

- ใช้ `||` และ `&&` **หยุดประเมินค่าตรงกลางเมื่อรู้ผลลัพธ์แล้ว**
- `admin?.name || user.name || "Anonymous"`
  1. `admin?.name` → undefined เพราะ admin = null
  2. undefined || user.name → "John" ✅
  3. ผลลัพธ์สุดท้าย: `"John"`
- `user && user.profile` → undefined เพราะ user.profile ไม่มี property นี้
- `?.` คือ **Optional Chaining** ป้องกัน error เมื่อ object เป็น null/undefined

---

## 7. Form Validation

### ผลลัพธ์

Form Validation:  
Valid user: { isValid: true, errors: [] }  
Invalid user: { isValid: false, errors: [ "Name must be at least 3 characters", "Valid email is required", "Must be 18 or older", "Password must be at least 6 characters", "Must agree to terms" ] }

### คำอธิบาย

- ฟังก์ชัน `validateRegistration(formData)` ตรวจสอบข้อมูล form
- ตรวจสอบ **name**, **email**, **age**, **password**, และ **agreeToTerms**
- เก็บ **errors** ลง array และคืนค่า object `{ isValid, errors }`
- user ถูกต้อง → `isValid: true`
- user ไม่ถูกต้อง → `isValid: false` พร้อม array ของข้อผิดพลาด

---

## สรุป

- Short-Circuit Evaluation ช่วยให้ **เขียนโค้ดปลอดภัยและสั้น**
- Form Validation แสดงตัวอย่างการตรวจสอบข้อมูลง่าย ๆ
- เหมาะสำหรับผู้เริ่มต้นเรียนรู้ **การควบคุม flow และตรวจสอบข้อมูล** ใน JavaScript

## Explanation of 04-loops.js

📁 **ไฟล์:** [04-loops.js](04-loops.js)  
ไฟล์นี้ฝึกการใช้งาน loop และ Array Methods ของ JavaScript  
โดยเน้นการประมวลผลข้อมูลด้วย `map`, `filter`, `reduce` และ Method Chaining  
พร้อมตัวอย่างการนำไปใช้จริงผ่านโจทย์วิเคราะห์คะแนนนักเรียน

---

## 9. Chaining Methods

### ผลลัพธ์

Method chaining:  
Even numbers squared: 2²=4, 4²=16, 6²=36, 8²=64, 10²=100  
Average: 30

### คำอธิบาย

- ใช้ **Method Chaining** (`filter → map → join`) เพื่อประมวลผลข้อมูลต่อเนื่อง
- `filter((n) => n % 2 === 0)`
  - คัดเลือกเฉพาะเลขคู่จาก array
  - ได้ `[2, 4, 6, 8, 10]`
- `map((n) => \`${n}²=${n \* n}\`)`
  - แปลงตัวเลขเป็น string แสดงค่ากำลังสอง
- `join(", ")`
  - รวม array ของ string ให้เป็นข้อความเดียว
- การคำนวณค่าเฉลี่ยใช้ `reduce` รวมค่าทั้งหมด  
  แล้วหารด้วย `length` ของ array
- แสดงให้เห็นแนวคิด **Functional Programming** ใน JavaScript

---

## 10. Challenge: Student Grades

### ผลลัพธ์

Challenge: Student Analysis  
Names: Alice, Bob, Charlie, Diana, Eve  
High scorers: Alice (95), Charlie (85), Diana (92), Eve (88)  
Class average: 87.00  
Top scorer: Alice (95)  
Summary (sorted):  
Alice: 95 (A)  
Diana: 92 (A)  
Eve: 88 (B)  
Charlie: 85 (B)  
Bob: 75 (C)

### คำอธิบาย

- ใช้ `map` เพื่อดึงรายชื่อนักเรียนออกมาแสดงผล
- ใช้ `filter` คัดกรองนักเรียนที่ได้คะแนนตั้งแต่ 85 ขึ้นไป
- ใช้ `reduce`
  - คำนวณค่าเฉลี่ยคะแนนของทั้งห้อง
  - หานักเรียนที่ได้คะแนนสูงสุด
- ใช้ `map` ร่วมกับ **ternary operator** เพื่อสร้างเกรด (A, B, C)
- ใช้ `sort` เรียงข้อมูลตามคะแนนจากมากไปน้อย
- เป็นตัวอย่างการประยุกต์ Array Methods กับข้อมูลจริง

---

## สรุป

ไฟล์ `04-loops.js` แสดงให้เห็น:

- การใช้ Array Methods แบบ chaining เพื่อประมวลผลข้อมูลหลายขั้นตอน
- การใช้ `map`, `filter`, `reduce`, และ `sort` กับข้อมูลเชิงโครงสร้าง
- การวิเคราะห์ข้อมูล เช่น ค่าเฉลี่ย คะแนนสูงสุด และการจัดเกรด

เหมาะสำหรับผู้เริ่มต้นที่ต้องการเข้าใจการจัดการข้อมูลด้วย Array  
และแนวคิดการเขียน JavaScript แบบ Functional

## Explanation of 05-integration.js

📁 **ไฟล์:** [05-integration.js](05-integration.js)  
ไฟล์นี้เป็นตัวอย่างการรวมหลายแนวคิดของ JavaScript เข้าด้วยกัน  
เพื่อสร้าง Quiz Application แบบง่าย ๆ โดยใช้ array, object, loop  
และ Array Methods ในการประมวลผลข้อมูลและสรุปผลคะแนน

---

## 5. Integration - Quiz Application

### ผลลัพธ์

🎯🎯 === QUIZ APPLICATION === 🎯🎯  
QUIZ RESULTS:  
Q1: What is 5 + 3?  
Your answer: 8  
✅ CORRECT

Q2: What is the capital of Thailand?  
Your answer: Phuket  
Correct answer: Bangkok  
❌ WRONG

Q3: What is the largest planet?  
Your answer: Jupiter  
✅ CORRECT

Q4: What is 2^8?  
Your answer: 256  
✅ CORRECT

Q5: Which is NOT a JavaScript data type?  
Your answer: class  
❌ WRONG

FINAL SCORE: 3/5 (60.0%)  
GRADE: D

FEEDBACK:  
📚📚 Good effort. Review the material and try again.

📊📊 STATISTICS:  
Total questions: 5  
Correct: 3  
Incorrect: 2  
Success rate: 60.0%

Answer breakdown:  
✅ Correct: 3  
❌ Incorrect: 2

---

### คำอธิบาย

โค้ดเริ่มจากการเตรียมข้อมูลคำถามไว้ใน array `quizzes` โดยแต่ละข้อเป็น object ที่มีคำถาม ตัวเลือก และตำแหน่งของคำตอบที่ถูกต้อง จากนั้นใช้ `forEach` วนลูปแต่ละคำถามและสุ่มคำตอบของผู้ใช้ด้วย `Math.random()` เพื่อจำลองการทำแบบทดสอบ ระบบจะเปรียบเทียบคำตอบที่สุ่มได้กับคำตอบที่ถูกต้อง แล้วบันทึกผลลัพธ์ลงใน array `results` พร้อมสถานะว่าตอบถูกหรือผิด

เมื่อได้ผลลัพธ์ครบทุกข้อแล้ว โปรแกรมจะแสดงผลทีละคำถาม พร้อมแสดงคำตอบที่ถูกต้องในกรณีที่ผู้ใช้ตอบผิด จากนั้นใช้ `filter` เพื่อนับจำนวนข้อที่ตอบถูก และคำนวณคะแนนรวมเป็นเปอร์เซ็นต์ พร้อมกำหนดเกรดด้วยเงื่อนไข `if...else` ตามช่วงคะแนน

ท้ายสุด โปรแกรมแสดง feedback ให้ผู้ใช้ตามระดับคะแนน และใช้ `reduce` สรุปจำนวนข้อที่ตอบถูกและผิด เพื่อแสดงสถิติภาพรวมของการทำแบบทดสอบทั้งหมด

---

## สรุป

ไฟล์ `05-integration.js` แสดงให้เห็น:

- การประยุกต์ใช้ array, object และ loop ในโปรแกรมเดียว
- การใช้ `forEach`, `filter`, `reduce` เพื่อประมวลผลข้อมูล
- การควบคุม flow ของโปรแกรมด้วยเงื่อนไข
- ตัวอย่าง Quiz Application ที่ใกล้เคียงการใช้งานจริง  
  เหมาะสำหรับผู้เริ่มต้นที่ต้องการฝึกเขียน JavaScript แบบครบกระบวนการตั้งแต่ต้นจนจบ
